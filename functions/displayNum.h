#ifndef DISPLAYNUM_H
#define DISPLAYNUM_H
// This block of preprocessor code is commonly known as the header guard, 
// avoids redefinition of blocks where multiple headers are included/ header included *in* a header
void displayDoubleNum (int);

#endif